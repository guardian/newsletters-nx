import { newsletterWorkflows } from './newsletter-workflows';

describe('newsletterWorkflows', () => {
	// it('should work', () => {
	// 	expect(newsletterWorkflows()).toEqual('newsletter-workflows');
	// });
});
