# Docs index

This folder is intentionally flat and unnumbered (matching `email-rendering`): stable file paths are preferred over numeric ordering that forces renames and link churn.

If ordering is ever introduced later, the first line of each doc remains its title heading so TOC tooling can key off that convention.
